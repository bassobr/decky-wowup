"""WoW Addons – Decky plugin backend that drives WowUp-CF headlessly.

Pure standard library (Python >= 3.9), no Decky imports outside log.py, so the
same code runs inside Decky Loader, in the SteamOS system Python (CLI) and in tests.
"""
